# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty scaffolding: it contains only `README.md` (a single-line title, "# Thunkin") and no source code, build configuration, dependency manifests, or tests. There is no established architecture, build/lint/test tooling, or set of conventions yet to document.

When code is added to this repository, update this file to cover:
- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and how major components fit together
- Project-specific conventions not obvious from the code itself
